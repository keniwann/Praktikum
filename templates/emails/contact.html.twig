{% extends 'base.html.twig' %}

{% block body %}
    <p>Hallo Keno,</p>

    <p>eine Person möchte über deine Landingpage in Kontakt mit dir treten.</p>
    <ul>
        <li>Name: {{ name }}</li>
        <li>Email: <a href="mailto:{{ from }}">{{ from }}</a></li>
        <li>Nachricht:
            <br><br>
            <span>{{ message }}</span>
        </li>
    </ul>
{% endblock %}
